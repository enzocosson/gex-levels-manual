{
  "rewrites": [
    {
      "source": "/api/gexbot/:path*",
      "destination": "https://api.gexbot.com/:path*"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
